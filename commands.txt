start emulator:

    emulator -avd Pixel_3a_API_30_x86

            "CAMERA_ROLL",
        "MANAGE_MEDIA"


install node module command 
    npm i --lehacy-peer-deps

changes in node_modules

1. DoubleClick :
    add activeOpacity = 0.8

2 Navigate to your node_modules/react-native/Libraries/Core/Timers/JSTimers.js file.

    Look for the variable MAX_TIMER_DURATION_MS

    Change its value to 10000 * 1000